# OpenClaw 操作说明手册

## 目录

- [基础操作](#基础操作)
- [初始配置](#初始配置)
- [技能管理](#技能管理)
- [Docker Compose 容器操作](#docker-compose-容器操作)
- [Docker Compose vs Docker 对比](#docker-compose-vs-docker-对比)
- [OpenClaw 操作注意事项](#openclaw-操作注意事项)
- [OpenClaw 操作建议](#openclaw-操作建议)
- [常见问题排查](#常见问题排查)

---

## 基础操作

### 查看日志

```bash
# 实时查看日志
docker compose logs -f openclaw-gateway

# 查看最近 100 行日志
docker compose logs --tail=100 openclaw-gateway

# 过滤错误日志
docker compose logs --tail=100 openclaw-gateway | grep -iE "error|fail|warn"

# 过滤特定关键词
docker compose logs -f openclaw-gateway | grep -i "kimi"
```

### 查看配置文件

```bash
# 认证配置
cat /home/robot/agi/CLAW_DATA/config/agents/main/agent/auth-profiles.json

# 示例输出
{
  "version": 1,
  "profiles": {
    "moonshot:default": {
      "type": "api_key",
      "provider": "moonshot",
      "key": "sk-xxx"
    }
  },
  "usageStats": {
    "moonshot:default": {
      "errorCount": 0,
      "lastUsed": 1773324413128
    }
  }
}
```

---

## 初始配置

### 首次安装配置向导

首次安装 OpenClaw 后，需要完成基本配置：

```bash
# 启动交互式配置向导
docker compose run --rm openclaw-cli configure

# 或者使用非交互式配置
docker compose run --rm openclaw-cli config
```

### 配置认证信息

OpenClaw 需要配置 AI 模型提供商的 API Key 才能正常工作：

```bash
# 方法 1: 直接编辑配置文件 (推荐)
nano /home/robot/agi/CLAW_DATA/config/agents/main/agent/auth-profiles.json

# 方法 2: 在容器内配置
docker compose exec openclaw-gateway openclaw configure
```

**支持的 AI 提供商**:

| 提供商 | Provider ID | 获取 API Key |
|--------|-------------|--------------|
| Moonshot (Kimi) | `moonshot` | https://platform.moonshot.cn/ |
| OpenAI | `openai` | https://platform.openai.com/ |
| Anthropic | `anthropic` | https://console.anthropic.com/ |
| DeepSeek | `deepseek` | https://platform.deepseek.com/ |
| Azure OpenAI | `azure` | Azure Portal |

### 配置文件结构

**认证配置** (`auth-profiles.json`):

```json
{
  "version": 1,
  "profiles": {
    "moonshot:default": {
      "type": "api_key",
      "provider": "moonshot",
      "key": "sk-你的API-KEY"
    },
    "openai:default": {
      "type": "api_key",
      "provider": "openai",
      "key": "sk-你的OpenAI-KEY"
    }
  }
}
```

**模型配置** (`config.json`):

```bash
# 查看当前模型配置
docker compose run --rm openclaw-cli config get models

# 设置默认模型
docker compose run --rm openclaw-cli config set models.defaultProfile "moonshot:default"
```

### 设备配对

首次使用需要在手机或浏览器配对：

```bash
# 查看配对状态
docker compose run --rm openclaw-cli devices list

# 生成配对码 (在 Control UI 中使用)
docker compose run --rm openclaw-cli devices pair

# 访问 Control UI
# 浏览器打开: http://localhost:18789
```

### 频道配置

配置消息接入频道（如 Kimi、Telegram、Discord 等）：

```bash
# 查看频道状态
docker compose run --rm openclaw-cli channels status

# 配置特定频道
docker compose run --rm openclaw-cli configure
```

### 配置验证

```bash
# 运行诊断检查配置
docker compose run --rm openclaw-cli doctor

# 验证配置文件
docker compose run --rm openclaw-cli config validate

# 查看完整配置
docker compose run --rm openclaw-cli config get
```

---

## 技能管理

### 技能概述

技能 (Skills) 是 OpenClaw 的扩展能力模块，提供各种专业功能：

- 📦 **内置技能**: OpenClaw 预置的技能（如 1password、weather 等）
- 🔧 **工作区技能**: 用户自定义开发的技能（如 stock-data）
- 📥 **安装技能**: 从 Clawhub 安装的第三方技能

### 查看技能

```bash
# 列出所有可用技能
docker compose run --rm openclaw-cli skills

# 输出示例:
# Skills (5/53 ready)
# ┌───────────┬──────────────────┬─────────────────────────┬────────────────────┐
# │ Status    │ Skill            │ Description             │ Source             │
# ├───────────┼──────────────────┼─────────────────────────┼────────────────────┤
# │ ✓ ready   │ 📦 stock-data    │ 获取股票市场数据...      │ openclaw-workspace │
# │ ✓ ready   │ 📦 vocabulary... │ 词汇学习系统...         │ openclaw-workspace │
# │ ✗ missing │ 🔐 1password     │ 1Password CLI 工具...   │ openclaw-bundled   │
# └───────────┴──────────────────┴─────────────────────────┴────────────────────┘
```

**状态说明**:

| 状态 | 含义 |
|------|------|
| `✓ ready` | 技能已就绪，可以使用 |
| `✗ missing` | 技能依赖缺失，需要安装 |
| `⚠ error` | 技能加载出错 |

### 技能目录结构

```
skills/
├── stock-data/              # 股票数据技能
│   ├── SKILL.md             # 技能说明文件
│   ├── stock_query.py       # 技能脚本
│   └── requirements.txt     # Python 依赖 (可选)
├── healthcheck/             # 健康检查技能
│   └── SKILL.md
└── ...
```

### 创建自定义技能

**步骤 1: 创建技能目录**

```bash
# 在 skills 目录下创建新技能
mkdir -p skills/my-skill
```

**步骤 2: 创建 SKILL.md 文件**

```markdown
# my-skill 技能名称

简短描述技能功能。

## 用途

- 功能 1
- 功能 2

## 使用方法

用户询问时如何触发此技能。

## 工具

### tool_name

工具描述。

参数:
- param1: 参数说明

示例:
\`\`\`bash
python3 skills/my-skill/script.py --param1 value
\`\`\`
```

**步骤 3: 添加脚本文件**

```bash
# 创建 Python 或 Shell 脚本
touch skills/my-skill/script.py
chmod +x skills/my-skill/script.py
```

**步骤 4: 验证技能**

```bash
# 重新加载技能列表
docker compose restart openclaw-gateway

# 检查技能状态
docker compose run --rm openclaw-cli skills | grep my-skill
```

### 技能配置

某些技能需要额外配置（如 API Key、路径等）：

**方法 1: 环境变量**

在 `docker-compose.yml` 中添加：

```yaml
services:
  openclaw-gateway:
    environment:
      - MY_SKILL_API_KEY=your-api-key
```

**方法 2: 配置文件**

在技能目录下创建 `config.json`:

```json
{
  "apiKey": "your-api-key",
  "endpoint": "https://api.example.com"
}
```

**方法 3: 交互式配置**

```bash
# 通过配置向导设置
docker compose run --rm openclaw-cli configure
```

### 技能依赖管理

技能可能依赖外部工具或库：

**Python 依赖**:

```bash
# 创建 requirements.txt
echo "requests>=2.28.0" > skills/my-skill/requirements.txt

# 安装依赖
pip3 install -r skills/my-skill/requirements.txt
```

**系统依赖**:

```bash
# 在技能 SKILL.md 中说明依赖
# 如: 需要安装 jq 命令行工具
sudo apt-get install jq
```

### 安装第三方技能

```bash
# 使用 Clawhub 搜索技能
npx clawhub search <skill-name>

# 安装技能
npx clawhub install <skill-name>

# 同步技能
npx clawhub sync
```

### 技能调试

```bash
# 手动测试技能脚本
python3 skills/stock-data/stock_query.py --type realtime --symbol 600519

# 查看技能加载日志
docker compose logs openclaw-gateway | grep -i skill

# 进入容器调试
docker compose exec openclaw-gateway bash
cd /app/skills/stock-data
python3 stock_query.py --help
```

### 技能帮助

每个技能都有详细说明文件，查看方式：

```bash
# 查看技能说明
cat skills/stock-data/SKILL.md

# 或在容器内查看
docker compose exec openclaw-gateway cat /app/skills/stock-data/SKILL.md
```

### 常用技能命令汇总

```bash
# 列出所有技能
docker compose run --rm openclaw-cli skills

# 查看特定技能详情
cat skills/<skill-name>/SKILL.md

# 测试技能脚本
python3 skills/<skill-name>/script.py --help

# 重启以加载新技能
docker compose restart openclaw-gateway

# 查看技能加载状态
docker compose logs --tail=50 openclaw-gateway | grep -i skill
```

---

## Docker Compose 容器操作

### 服务名与容器名

**重要概念**: `docker compose exec openclaw-gateway bash` 中的 `openclaw-gateway` 是 **服务名 (Service Name)**，不是容器名。

#### 服务名 vs 容器名

| 项目 | 服务名 | 容器名 |
|------|--------|--------|
| **定义位置** | `docker-compose.yml` 中定义 | Docker 自动生成 |
| **示例** | `openclaw-gateway` | `openclaw-openclaw-gateway-1` |
| **命名规则** | 用户自定义 | `{项目名}-{服务名}-{实例号}` |
| **是否可变** | 修改配置文件即可 | 需指定 `container_name` |
| **用途** | Docker Compose 命令使用 | Docker 原生命令使用 |

#### 如何确定服务名？

查看 `docker-compose.yml` 文件：

```yaml
services:
  openclaw-gateway:    # ← 这就是服务名
    image: openclaw:local
    ports:
      - "18789:18789"
  openclaw-cli:        # ← 另一个服务名
    image: openclaw:local
```

#### 查看服务名与容器名的对应关系

```bash
# 使用 docker compose 查看 (显示服务名)
docker compose ps

# 输出示例:
# NAME                          SERVICE            IMAGE
# openclaw-openclaw-gateway-1   openclaw-gateway   openclaw:local

# 使用 docker ps 查看 (显示容器名)
docker ps --format "table {{.Names}}\t{{.Image}}"

# 输出示例:
# NAMES                           IMAGE
# openclaw-openclaw-gateway-1     openclaw:local
```

#### Docker Compose 命令 vs Docker 原生命令

```bash
# Docker Compose 命令 - 使用服务名
docker compose exec openclaw-gateway bash    # ✅ 推荐
docker compose logs openclaw-gateway         # ✅ 推荐
docker compose restart openclaw-gateway      # ✅ 推荐

# Docker 原生命令 - 使用容器名
docker exec -it openclaw-openclaw-gateway-1 bash    # ✅ 可用但繁琐
docker logs openclaw-openclaw-gateway-1             # ✅ 可用但繁琐
docker restart openclaw-openclaw-gateway-1          # ✅ 可用但繁琐
```

#### 为什么 Docker Compose 更方便？

1. **无需记忆完整容器名**: 服务名简短易记
2. **项目隔离**: Docker Compose 自动识别当前项目的容器
3. **多实例支持**: 服务名可以自动扩展到多个实例
4. **配置集中管理**: 所有配置在 `docker-compose.yml` 中

#### 自定义容器名 (可选)

如果需要指定容器名，可在 `docker-compose.yml` 中添加：

```yaml
services:
  openclaw-gateway:
    container_name: my-openclaw  # 自定义容器名
    image: openclaw:local
```

**注意**: 自定义容器名后，只能运行一个实例，不利于扩容。

### 进入容器

```bash
# 进入 gateway 容器 (交互式 bash)
docker compose exec openclaw-gateway bash

# 如果 bash 不可用，使用 sh
docker compose exec openclaw-gateway sh

# 以 root 用户进入
docker compose exec -u root openclaw-gateway bash

# 进入 CLI 容器 (注意: CLI 容器通常运行后退出)
docker compose run --rm openclaw-cli bash
```

### 容器内可执行的操作

进入容器后，可以执行以下操作：

```bash
# 1. 系统信息
uname -a                    # 查看系统信息
cat /etc/os-release         # 查看系统版本
env                         # 查看环境变量
df -h                       # 查看磁盘空间
free -m                     # 查看内存使用

# 2. OpenClaw 命令 (在 gateway 容器内)
openclaw --version          # 查看版本
openclaw config get         # 查看配置
openclaw channels status    # 查看频道状态
openclaw devices list       # 查看设备列表
openclaw plugins list       # 查看插件列表
openclaw doctor             # 运行诊断

# 3. 网络诊断
curl http://localhost:18789/healthz    # 健康检查
netstat -tlnp                          # 查看端口监听
ping google.com                        # 测试网络连通性

# 4. 进程管理
ps aux                      # 查看所有进程
top                         # 实时进程监控
kill -9 <pid>               # 终止进程 (谨慎使用)

# 5. 日志查看 (容器内)
tail -f /var/log/openclaw/*.log    # 查看日志文件

# 6. 文件操作
ls -la /data/               # 查看数据目录
cat /etc/openclaw/config.json   # 查看配置文件
```

### 容器关闭

```bash
# 停止所有服务 (保留容器)
docker compose stop

# 停止特定服务
docker compose stop openclaw-gateway

# 停止并删除容器 (保留数据卷)
docker compose down

# 停止并删除容器和数据卷 (危险! 会删除数据)
docker compose down -v

# 优雅停止 (等待进程完成)
docker compose stop -t 30 openclaw-gateway  # 等待30秒超时
```

### 容器重启

```bash
# 重启所有服务
docker compose restart

# 重启特定服务
docker compose restart openclaw-gateway

# 强制重建并启动 (代码更新后)
docker compose up -d --build

# 拉取最新镜像并重启
docker compose pull && docker compose up -d
```

### 其他常用命令

```bash
# 查看服务状态
docker compose ps

# 查看资源使用
docker compose top

# 查看网络
docker network ls
docker network inspect openclaw_default

# 查看数据卷
docker volume ls
docker volume inspect openclaw_data

# 复制文件到容器
docker compose cp ./local-file.txt openclaw-gateway:/tmp/

# 从容器复制文件
docker compose cp openclaw-gateway:/etc/openclaw/config.json ./
```

---

## Docker Compose vs Docker 对比

### 相同点

| 功能 | Docker | Docker Compose |
|------|--------|----------------|
| 容器管理 | ✅ | ✅ |
| 镜像操作 | ✅ | ✅ |
| 网络管理 | ✅ | ✅ |
| 数据卷 | ✅ | ✅ |
| 日志查看 | ✅ | ✅ |
| 进入容器 | ✅ | ✅ |

### 不同点

| 特性 | Docker | Docker Compose |
|------|--------|----------------|
| **配置方式** | 命令行参数 | YAML 配置文件 |
| **多容器管理** | 需要手动管理每个容器 | 一键管理所有容器 |
| **依赖关系** | 手动处理 | 自动处理 depends_on |
| **网络创建** | 手动创建 | 自动创建 |
| **环境变量** | -e 参数逐个传递 | .env 文件批量管理 |
| **重启策略** | 单独设置 | 统一配置 |
| **服务发现** | 手动配置 IP | 自动服务名解析 |

### 命令对照表

```bash
# 启动容器
docker run -d --name openclaw-gateway -p 18789:18789 openclaw/openclaw
docker compose up -d

# 停止容器
docker stop openclaw-gateway              # 使用容器名
docker compose stop                       # 停止所有服务 (或指定服务名)
docker compose stop openclaw-gateway      # 使用服务名

# 删除容器
docker rm openclaw-gateway                # 使用容器名
docker compose down                       # 停止并删除所有服务

# 查看日志
docker logs -f openclaw-gateway           # 使用容器名
docker compose logs -f openclaw-gateway   # 使用服务名

# 进入容器
docker exec -it openclaw-gateway bash     # 使用容器名
docker compose exec openclaw-gateway bash # 使用服务名

# 重启容器
docker restart openclaw-gateway           # 使用容器名
docker compose restart openclaw-gateway   # 使用服务名

# 查看状态
docker ps                                 # 显示容器名
docker compose ps                         # 显示服务名
```

**关键区别**: Docker 原生命令使用容器名，Docker Compose 命令使用服务名。

### 何时使用哪个?

| 场景 | 推荐 |
|------|------|
| 单容器简单部署 | Docker |
| 多容器应用 | Docker Compose |
| 开发环境 | Docker Compose |
| CI/CD 流水线 | Docker 或 Docker Compose |
| 生产环境复杂应用 | Docker Compose 或 Kubernetes |

---

## OpenClaw 操作注意事项

### 1. 数据持久化

- 所有数据存储在 `/home/robot/agi/CLAW_DATA` 目录
- 删除容器不会删除数据，但 `docker compose down -v` 会删除数据卷
- 定期备份 `CLAW_DATA` 目录

### 2. 端口占用

- 默认端口: 18789 (HTTP), 18790 (WebSocket)
- 确保端口未被占用: `ss -tlnp | grep 18789`
- 修改端口需编辑 `docker-compose.yml`

### 3. API Key 安全

- 不要在日志中暴露 API Key
- 不要将 `auth-profiles.json` 提交到版本控制
- 定期轮换 API Key

### 4. 容器资源限制

- 默认无资源限制，可能占用大量内存
- 建议在 `docker-compose.yml` 中设置资源限制:
```yaml
services:
  openclaw-gateway:
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '2'
```

### 5. 网络模式

- 默认使用 bridge 模式
- 如需访问宿主机服务，使用 `host` 模式或 `network_mode: host`

### 6. 时区设置

- 容器默认 UTC 时区
- 设置中国时区: 在 `docker-compose.yml` 添加:
```yaml
environment:
  - TZ=Asia/Shanghai
```

### 7. 日志管理

- Docker 日志会无限增长
- 设置日志轮转:
```yaml
services:
  openclaw-gateway:
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
```

### 8. 插件兼容性

- 更新 OpenClaw 版本后，检查插件兼容性
- 使用 `openclaw plugins list` 确认插件状态
- 不兼容的插件可能导致启动失败

### 9. 认证配置

- API Key 配置在 `auth-profiles.json` 中
- 不能通过 `config set` 命令设置，需直接编辑文件
- 修改后需重启容器

### 10. 备份恢复

```bash
# 备份
tar -czvf openclaw-backup-$(date +%Y%m%d).tar.gz /home/robot/agi/CLAW_DATA

# 恢复
tar -xzvf openclaw-backup-20260312.tar.gz -C /
```

---

## OpenClaw 操作建议

### 1. 日常维护

```bash
# 每天检查服务状态
docker compose ps

# 定期查看错误日志
docker compose logs --tail=50 openclaw-gateway | grep -i error

# 定期清理未使用的镜像
docker image prune -f
```

### 2. 监控告警

```bash
# 使用健康检查端点
curl -f http://localhost:18789/healthz || alert "OpenClaw is down"

# 检查容器是否健康
docker inspect --format='{{.State.Health.Status}}' openclaw-gateway-1
```

### 3. 版本更新

```bash
# 1. 备份数据
tar -czvf backup.tar.gz /home/robot/agi/CLAW_DATA

# 2. 拉取最新代码
git pull --rebase

# 3. 重建并启动
docker compose down && docker compose up -d --build

# 4. 验证
docker compose ps
curl http://localhost:18789/healthz
```

### 4. 性能优化

- 使用 SSD 存储数据目录
- 为容器分配足够的内存 (建议 2GB+)
- 使用本地镜像仓库加速拉取
- 减少不必要的插件

### 5. 安全加固

```bash
# 限制容器权限
# 在 docker-compose.yml 中添加:
security_opt:
  - no-new-privileges:true
read_only: true  # 只读文件系统 (需要配置 tmpfs)

# 不要使用 root 运行
user: "1000:1000"
```

### 6. 日志排查技巧

```bash
# 按时间过滤
docker compose logs --since="2024-01-01T00:00:00" openclaw-gateway

# 实时监控多个关键词
docker compose logs -f openclaw-gateway | grep -E "error|warn|kimi"

# 导出日志到文件
docker compose logs --tail=1000 openclaw-gateway > gateway.log
```

### 7. 网络问题排查

```bash
# 检查容器网络
docker compose exec openclaw-gateway ping -c 3 google.com

# 检查 DNS 解析
docker compose exec openclaw-gateway nslookup api.moonshot.cn

# 检查端口
docker compose exec openclaw-gateway netstat -tlnp
```

### 8. 开发调试

```bash
# 进入调试模式
docker compose run --rm openclaw-cli --verbose config get

# 查看环境变量
docker compose exec openclaw-gateway env | sort

# 实时监控资源
docker stats openclaw-gateway-1
```

### 9. 多环境管理

```bash
# 使用不同的 compose 文件
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

# 使用环境变量文件
cp .env.example .env
# 编辑 .env 文件
docker compose up -d
```

### 10. 故障恢复

```bash
# 完全重置 (保留数据)
docker compose down
docker compose pull
docker compose up -d

# 数据恢复
docker compose down
rm -rf /home/robot/agi/CLAW_DATA
tar -xzvf backup.tar.gz -C /
docker compose up -d
```

---

## 常见问题排查

### 容器无法启动

```bash
# 1. 查看错误日志
docker compose logs openclaw-gateway

# 2. 检查端口占用
ss -tlnp | grep 18789

# 3. 检查数据目录权限
ls -la /home/robot/agi/CLAW_DATA

# 4. 检查配置文件
cat /home/robot/agi/CLAW_DATA/config/agents/main/agent/auth-profiles.json
```

### API Key 无效

```bash
# 1. 检查配置
cat /home/robot/agi/CLAW_DATA/config/agents/main/agent/auth-profiles.json

# 2. 更新 Key (直接编辑文件)
nano /home/robot/agi/CLAW_DATA/config/agents/main/agent/auth-profiles.json

# 3. 重启服务
docker compose restart openclaw-gateway
```

### 网络连接问题

```bash
# 1. 检查容器网络
docker network inspect openclaw_default

# 2. 检查防火墙
sudo ufw status
sudo iptables -L -n | grep 18789

# 3. 测试端口
curl -v http://127.0.0.1:18789/healthz
```

### 内存不足

```bash
# 1. 检查内存使用
docker stats

# 2. 清理缓存
docker system prune -f

# 3. 重启容器
docker compose restart openclaw-gateway
```

---

## 快速命令参考

### 服务管理

```bash
docker compose up -d                    # 启动
docker compose stop                     # 停止
docker compose restart                  # 重启
docker compose down                     # 停止并删除容器
docker compose ps                       # 查看状态
```

### 日志查看

```bash
docker compose logs -f openclaw-gateway # 实时日志
docker compose logs --tail=100 openclaw-gateway  # 最近日志
docker compose logs --tail=50 openclaw-gateway | grep -iE "error|warn"  # 错误日志
```

### 进入容器

```bash
docker compose exec openclaw-gateway bash   # 进入 gateway
docker compose run --rm openclaw-cli bash   # 进入 CLI
```

### 配置管理

```bash
docker compose run --rm openclaw-cli configure          # 交互式配置向导
docker compose run --rm openclaw-cli config get         # 查看所有配置
docker compose run --rm openclaw-cli config get models  # 查看模型配置
docker compose run --rm openclaw-cli config get auth    # 查看认证配置
docker compose run --rm openclaw-cli config validate    # 验证配置
```

### 技能管理

```bash
docker compose run --rm openclaw-cli skills             # 列出所有技能
cat skills/stock-data/SKILL.md                          # 查看技能说明
python3 skills/stock-data/stock_query.py --help         # 技能脚本帮助
npx clawhub search <name>                               # 搜索技能
npx clawhub install <name>                              # 安装技能
```

### 设备与频道

```bash
docker compose run --rm openclaw-cli devices list       # 设备列表
docker compose run --rm openclaw-cli devices pair       # 生成配对码
docker compose run --rm openclaw-cli channels status    # 频道状态
docker compose run --rm openclaw-cli plugins list       # 插件列表
```

### 诊断与健康检查

```bash
docker compose run --rm openclaw-cli doctor             # 运行诊断
curl http://127.0.0.1:18789/healthz                     # 健康检查
docker compose exec openclaw-gateway openclaw doctor    # 容器内诊断
```

### 配置文件位置

```bash
# 认证配置
/home/robot/agi/CLAW_DATA/config/agents/main/agent/auth-profiles.json

# 技能目录
/home/robot/agi/openclaw/skills/

# Docker Compose 配置
/home/robot/agi/openclaw/docker-compose.yml
```
